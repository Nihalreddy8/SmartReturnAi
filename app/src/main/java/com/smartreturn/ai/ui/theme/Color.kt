package com.smartreturn.ai.ui.theme

import androidx.compose.ui.graphics.Color

// === SmartReturn AI Design System ===
// Dark, premium AI aesthetic — inspired by iQOO ecosystem

// Primary — Electric Cyan
val CyanPrimary = Color(0xFF4FC3F7)
val CyanPrimaryVariant = Color(0xFF0288D1)
val CyanOnPrimary = Color(0xFF003548)

// Secondary — Violet/Indigo accent
val VioletSecondary = Color(0xFF7C4DFF)
val VioletSecondaryVariant = Color(0xFF651FFF)

// Backgrounds
val BackgroundDeep = Color(0xFF050A18)      // Near-black deep navy
val BackgroundSurface = Color(0xFF0D1426)   // Card backgrounds
val BackgroundElevated = Color(0xFF162038)  // Elevated surfaces

// Surface variants
val SurfaceContainer = Color(0xFF1A2640)
val SurfaceContainerHigh = Color(0xFF1F2E4A)
val OutlineColor = Color(0xFF2A3F5F)
val OutlineVariant = Color(0xFF1E2E45)

// Text
val TextPrimary = Color(0xFFE8F4FD)
val TextSecondary = Color(0xFF8BA8C4)
val TextTertiary = Color(0xFF4A6480)

// Confidence colors
val ConfidenceHigh = Color(0xFF4FC3F7)      // 90-100% — Cyan
val ConfidenceMedium = Color(0xFFFFB74D)    // 70-89%  — Amber
val ConfidenceLow = Color(0xFFEF5350)       // <70%    — Red

// Status
val StatusActive = Color(0xFF4CAF50)        // Green — item present
val StatusMissing = Color(0xFFFF5722)       // Deep orange — missing
val StatusUnknown = Color(0xFF607D8B)       // Grey — unknown

// Alert
val AlertWarning = Color(0xFFFF6D00)
val AlertBackground = Color(0xFF1A0D00)

// Memory timeline
val TimelineConnector = Color(0xFF2A3F5F)
val TimelineNode = Color(0xFF4FC3F7)
